#pragma once

#include "../../../common/UuidId.hpp"

// =============================================================================
// TeamId — チームIDを表す値オブジェクト
// UUID v4 形式のみ受け付ける。UserId と型レベルで区別される。
// =============================================================================
struct TeamTag {};
using TeamId = UuidId<TeamTag>;
