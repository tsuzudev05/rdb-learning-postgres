#pragma once

#include "../../../common/UuidId.hpp"

// =============================================================================
// ObjectiveId — 目標（Objective）IDを表す値オブジェクト
// =============================================================================
struct ObjectiveTag {};
using ObjectiveId = UuidId<ObjectiveTag>;
