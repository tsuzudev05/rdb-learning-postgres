-- =============================================================================
-- 初期化スクリプト（PostgreSQL docker-entrypoint-initdb.d）
--
-- テーブル定義は setup.sh から 05_DDD統合/schema.sql で行う。
-- ここでは uuid-ossp 拡張のみ有効化する。
-- =============================================================================
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
